server:
  port: 9092

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/YarismaMainDB
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
